# 编译警告统计

**总数**: 163 个警告

## 警告分类

### 1. unused imports (约 30 个)
- `Html`, `Selector` - src/tools/network/search_engine.rs
- `std::path::Path`, `Path` - 多个文件
- `task_decomposer::TaskDecomposer`
- `iteration_tracker::IterationTracker`
- `render_prompt`, `Context`, `template::PromptTemplate`
- `manager::PromptTemplateManager`, `renderer::PromptRenderer`
- `serde_json::Value`
- `ParameterDoc`, `ToolCallExample`
- `ToolBox`, `ToolDefinition`
- `registry::ToolRegistry`
- `skills_manager::SkillsManager`
- `selector::ToolSelector`
- `anyhow::Result`
- `MessageType`, `OptimizationStrategy`, `Stage`, `StepStatus`, `Step`, `WorkflowResult`
- `OptimizationResult`, `OptimizationStats`
- `CommandResult`, `OrchestratorCommand`, `OrchestratorConfig`, `OrchestratorState`, `ProcessedInput`
- `RoleState`
- `ExecutionRecord`, `StageStatus`, `WorkflowStatus`

### 2. unused variables (约 5 个)
- `workflow_id`

### 3. dead_code / never used (约 20 个)
- field `git_workflow` is never read
- associated function `with_monitor`
- variant `ApiError`
- methods `search_with_searxng`, `search_with_duckduckgo`, `search_ddg_inner`
- function `parse_duckduckgo_results`
- field `max_retries`
- fields `url`, `method`, `timestamp`
- methods `get_stats`, `get_recent_logs`, `get_failure_rate`, `clear_stats`, `clear_logs`
- field `last_check`
- methods `get_health_status`, `clear_cache`, `cache_size`
- variants `SensitivePath`, `OutOfCwd`, `PathTooLong`
- fields `max_path_length`, `sensitive_paths`
- enum `UrlSafety`
- methods `is_safe`, `into_result`
- functions `check_ip_safety`, `validate_save_path`

## 清理优先级

### 高优先级（快速修复）
1. **unused imports** - 直接删除即可
2. **unused variables** - 添加 `_` 前缀或使用 `#[allow(dead_code)]`

### 中优先级（需要判断）
1. **dead_code** - 判断是否需要保留
   - 如果是公共 API 或未来功能，添加 `#[allow(dead_code)]`
   - 如果是废弃代码，直接删除

## 建议的修复顺序

1. 先修复 `src/tools/network/` 目录
2. 然后 `src/prompt_engineering/`
3. 然后 `src/context/`
4. 最后处理其他目录

---
*由 coderA 创建，供 coderB 参考*
